/**
 * Implement createAdder(n) that returns a function which adds n to its argument.
 * Implement once(fn) that returns a function calling fn only the first time.
 */
export function createAdder(n: number): (x: number) => number {
  // TODO
  throw new Error("not implemented");
}

export function once<T extends (...args: never[]) => unknown>(fn: T): T {
  // TODO
  throw new Error("not implemented");
}
