/** Return indices i<j such that nums[i]+nums[j]===target, or null. O(n) with a Map. */
export function twoSum(nums: number[], target: number): [number, number] | null {
  throw new Error("not implemented");
}
